{
  "scripts": {
    "emulators:docker": "docker compose up --build",
    "emulators:docker:detach": "docker compose up --build -d",
    "seed": "ts-node seed.ts",
    "challenge": "ts-node challenge-run.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "firebase": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "firebase-admin": "^13.7.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.2",
    "vitest": "^3.0.0"
  }
}
